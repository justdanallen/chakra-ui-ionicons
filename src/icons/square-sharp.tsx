import React from "react";
import { Icon, IconProps } from '@chakra-ui/icon';

export const SquareSharp = (props: IconProps) => (
  <Icon viewBox='0 0 512 512' {...props}>
    <path  d='M48 48h416v416H48z'/>
  </Icon>
);